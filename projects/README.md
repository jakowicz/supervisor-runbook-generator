# Generated projects

Each `supervisor generate-runbooks "<brief>"` invocation creates one project
workspace here, named from the brief. Its `runbooks/` directory contains that
project's generated implementation contracts.
