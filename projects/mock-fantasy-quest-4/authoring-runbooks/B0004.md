---
task_id: B0004
sequence: 5
title: Author content-validation and local recovery contracts
browser_impact: not_applicable
playwright_spec:
---

## Objective

After B0003, create only `../runbooks/R0005.md` and `../runbooks/R0006.md`
for IMP-0005 and IMP-0006; retain GATE-0003.

## Context packet

Read the B0004/R0005/R0006 manifest records, the two catalogue records,
`../PROJECT_BRIEF.md`, `03-technical-contract.md#versioned-content-and-save-schemas`,
`03-technical-contract.md#persistence-migration-and-failure-semantics`,
`02-feature-model.md#save-recovery-and-offline-persistence`, and
`04-experience-contract.md#empty-error-recovery-destructive-and-privacy-sensitive-states`.

## Output list

- `../runbooks/R0005.md` — IMP-0005; dependencies R0001,R0003,R0004.
- `../runbooks/R0006.md` — IMP-0006; dependencies R0002,R0003,R0004.

## Required authoring content

Use standard front matter, exact manifest provenance (`authoring_batch: B0004`),
and `asset_impact: not_applicable` with blank `asset_ids`. Each output must
cover objective, dependencies, requirements, ownership, behaviour, non-goals,
platform differences, verification/evidence, observable acceptance criteria,
and an `## Asset assessment` forbidding incidental asset creation.

## Validation procedure

Never overwrite a conflicting R file. Load and compare both files with the
manifest, including provenance and dependencies, then verify required sections
and GATE-0003 retention.

## Checkpoint and update procedure

Update B0004 only after both structural checks pass; preserve IDs and repair
only the failed output if resumed.

## Stop condition

Stop after two outputs. Do not select browser storage or claim the save gate is approved.

## Acceptance criteria

- R0005 and R0006 are the only generated contracts and match their two IMP allocations.
- Both contracts retain the save/storage gate and make no incidental asset work.
- Loader, provenance, dependency, ownership, and required-section validation completes before checkpointing.
- A mismatch stops rather than overwrites an R contract.
